# Roadmap

- build drift indication on top of the new edge-profile baseline instead of skipping directly to full tracking workflows
- align later tracking contracts with ROI, focus, and overlay model decisions
- defer crack-tip heuristics until the profile baseline and drift/result quality criteria are clearer
