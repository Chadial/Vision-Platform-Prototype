# Roadmap

- keep the first stored-image focus-report baseline narrow and stable
- extend beyond `.pgm` / `.ppm` only when a shared reusable loader boundary is clearly justified
- add richer offline analysis or export only after the first report-oriented baseline proves useful
