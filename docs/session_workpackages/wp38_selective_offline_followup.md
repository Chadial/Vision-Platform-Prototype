# Selective Offline Follow-Up

## Purpose

This work package defines the next optional selective-expansion candidate after the current hardening and handover-oriented follow-ups.

Closure lane:

- Post-Closure Python Baseline / Selective Expansion

Slice role:

- optional offline utility extension

Scope level:

- current offline and measurement follow-up only when justified

Its purpose is to preserve one ready next step for offline / measurement utility only if a real user need appears after the current post-closure hardening and handover slices.

This package should be read as:

- conditional selective expansion
- not a default next slice
- not broad offline workstation growth

## Branch

- intended branch: `feature/selective-offline-followup`
- activation state: landed

## Scope

Included:

- derive one narrow offline value-add only when a concrete user need is present
- prefer reuse of the existing traceability and postprocess baseline

Excluded:

- broad offline explorer work
- report-framework redesign
- generalized artifact browser behavior

What this package does not close:

- full offline workstation direction

## Session Goal

Keep one clearly bounded offline-expansion option visible without making it the default planning lane.

Landed outcome:

- the compact offline focus-report bundle now exposes one additive summary line above the existing per-image path
- that summary line reports entry count, traceability-join coverage, and the current highest-score image
- the existing list-return path and the narrow text-report character remain unchanged

