# Status Report

## Purpose

This document is a compact secondary reading aid.

It is not the authoritative repository status document.

Use these files as the real sources of truth:

- `docs/STATUS.md`
- `docs/WORKPACKAGES.md`
- `docs/TARGET_MAP.md`

## Current Reading

Read the repository currently as:

- phase: `Usable Camera Subsystem / Pre-Product Baseline`
- first product goal: host-steerable running `Vision App / wxShell`
- product form: `Hybrid Companion`
- current priority: host commands, shell reflection, shell settings support, and practical execution of the confirmed workflows
- next structural direction: headless kernel after local and host usability are strong enough

## Confirmed Functional Workflows

- `Delamination Recording`
- `Geometry Capture`
- `Setup / Focus / ROI Adjustment`

Technical anchor flows such as snapshot, bounded recording, and interval capture remain useful validation and operator recipes, but they are not the primary product-reading layer.

## Rule

If this document and `docs/STATUS.md` disagree, `docs/STATUS.md` wins.

If this document and `docs/WORKPACKAGES.md` disagree on next work, `docs/WORKPACKAGES.md` wins.

## Recommended Use

Use this file only when you want one short human-readable orientation summary.

Do not update this file instead of `docs/STATUS.md` or `docs/WORKPACKAGES.md`.
